apiVersion: v1
kind: Service
metadata:
  name: aceest-service-a
  labels:
    project: aceest-fitness
spec:
  selector:
    app: aceest-ab
    track: a
  ports:
  - port: 80
    targetPort: 5000
  type: ClusterIP