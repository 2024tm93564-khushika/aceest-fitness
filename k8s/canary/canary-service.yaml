apiVersion: v1
kind: Service
metadata:
  name: aceest-canary-service
  labels:
    project: aceest-fitness
spec:
  selector:
    app: aceest-canary
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5000
  type: NodePort